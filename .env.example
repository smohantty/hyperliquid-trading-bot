# Hyperliquid Exchange Configuration
# Required: Path to wallet config JSON file
# JSON format: { "master_account_address": "...", "agent_private_key": { "mainnet": "...", "testnet": "..." } }
HYPERLIQUID_WALLET_CONFIG_FILE=./wallet_config.json

# Optional: Network Selection (Default: mainnet)
# Options: mainnet, testnet
HYPERLIQUID_NETWORK=mainnet

# Optional: Telegram Reporting
# Leave empty to disable Telegram notifications
# You can provide a JSON config file path via TELEGRAM_CONFIG_FILE
# JSON format: { "bot_token": "...", "chat_id": "..." }
# TELEGRAM_CONFIG_FILE=/run/secrets/telegram_config.json

# Alternatively, set individual variables (supports _FILE suffix for secrets)
# TELEGRAM_BOT_TOKEN=
# TELEGRAM_CHAT_ID=

# Optional: WebSocket Server Configuration
# WS_PORT: Port to listen on (Default: 9000). overridden by --ws-port CLI arg.
# WS_HOST: Bind address (Default: 0.0.0.0). Set to 127.0.0.1 to restrict to localhost.
# WS_PORT=9000
# WS_HOST=0.0.0.0
