# Example configuration for local development
# Rename this file to .env.local

# Option 1: Full URL override (Best for connecting to a different machine)
# VITE_WS_URL=ws://192.168.0.25:9000

# Option 2: Port override (Best for running on same machine with non-default port)
# connected to ws://{current_hostname}:{VITE_WS_PORT}
# VITE_WS_PORT=9001
